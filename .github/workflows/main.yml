name: fist_try

on: push
jobs:
  work1:
    runs-on: ubuntu-latest

    steps:
    - name: checkout Repo
      uses: actions/checkout@v4
    - name: systemupdate
      run: sudo apt update -y
    - name: install tree
      run: sudo apt install tree
    - name: cahnge_dir
      run: cd ~
    - name: folder
      run: mkdir newmohammad
            cd new
    - name: tree1
      run: tree